const timeline=document.getElementById("timeline"),searchDialog=document.getElementById("searchDialog"),searchInput=document.getElementById("searchInput"),searchResults=document.getElementById("searchResults"),dateDialog=document.getElementById("dateDialog"),supportDialog=document.getElementById("supportDialog");
function formatDate(s){const[y,m,d]=s.split("-");return `${d}/${m}/${y}`}
function cardTemplate(i){return `<article class="event-card" id="${i.id}"><div class="meta"><span class="badge">${i.category}</span><span class="badge verified">${i.status}</span></div><h3>${i.title}</h3><p>${i.summary}</p><div class="source-row"><a href="${i.sourceUrl}" target="_blank" rel="noopener noreferrer">${i.sourceType} ✓ · ${i.sourceName}</a><button class="details-btn" data-details="${i.id}">Ver →</button></div><div class="details" id="details-${i.id}"><h4>DATOS DEL REGISTRO</h4><ul><li>Fecha del hecho: ${formatDate(i.eventDate)}</li><li>Fecha de publicación: ${formatDate(i.publishedDate)}</li><li>Estado: ${i.status}</li><li>Relacionados: ${i.related.join(", ")}</li></ul>${i.extraSources.length?`<div class="secondary-sources"><h4>OTRAS FUENTES</h4>${i.extraSources.map(([n,u])=>`<a href="${u}" target="_blank" rel="noopener noreferrer">${n} ↗</a>`).join("")}</div>`:""}</div></article>`}
function renderTimeline(){const groups=[["government","GOBIERNO"],["opposition","OPOSICIÓN"],["state","ESTADO Y PAÍS"]];timeline.innerHTML=`<div class="day-node">07 AGO 2026 · VERIFICADO ✓</div>`+groups.map(([k,l])=>`<section class="group ${k}"><div class="group-label">${l}</div>${events.filter(e=>e.group===k).map(cardTemplate).join("")}</section>`).join("");document.querySelectorAll(".details-btn").forEach(b=>b.addEventListener("click",()=>{const p=document.getElementById(`details-${b.dataset.details}`);p.classList.toggle("open");b.textContent=p.classList.contains("open")?"Cerrar ↑":"Ver →"}))}
function doSearch(q){q=q.trim().toLowerCase();if(!q){searchResults.innerHTML="";return}const hits=events.filter(e=>[e.title,e.summary,e.category,e.sourceName,...e.related].join(" ").toLowerCase().includes(q));const gh=Object.entries(glossary).filter(([t,d])=>`${t} ${d}`.toLowerCase().includes(q));searchResults.innerHTML=hits.map(e=>`<button class="search-hit" data-jump="${e.id}"><strong>${e.title}</strong><span>${e.groupLabel} · ${e.category}</span></button>`).join("")+gh.map(([t,d])=>`<div class="search-hit"><strong>${t}</strong><span>${d}</span></div>`).join("")||`<div class="search-hit"><strong>Sin resultados</strong><span>Prueba otro término.</span></div>`;document.querySelectorAll("[data-jump]").forEach(b=>b.addEventListener("click",()=>{searchDialog.close();document.getElementById(b.dataset.jump)?.scrollIntoView({behavior:"smooth",block:"center"})}))}
document.getElementById("openSearch").addEventListener("click",()=>searchDialog.showModal());document.getElementById("navSearch").addEventListener("click",()=>searchDialog.showModal());searchInput.addEventListener("input",e=>doSearch(e.target.value));document.getElementById("goToDate").addEventListener("click",()=>dateDialog.showModal());document.getElementById("dateSubmit").addEventListener("click",e=>{e.preventDefault();dateDialog.close();document.querySelector(".day-node")?.scrollIntoView({behavior:"smooth"})});document.getElementById("supportBtn").addEventListener("click",()=>supportDialog.showModal());document.querySelectorAll(".nav-item").forEach(b=>b.addEventListener("click",()=>{document.querySelectorAll(".nav-item").forEach(x=>x.classList.remove("active"));b.classList.add("active");if(["hoy","dia"].includes(b.dataset.section))window.scrollTo({top:0,behavior:"smooth"});if(b.dataset.section==="entiende"){searchDialog.showModal();searchInput.value="presunción de inocencia";doSearch(searchInput.value)}}));renderTimeline();
